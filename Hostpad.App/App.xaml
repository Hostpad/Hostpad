<Application x:Class="Hostpad.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Dark" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>

            <FontFamily x:Key="FluentIconFont">pack://application:,,,/Wpf.Ui;component/Resources/Fonts/#FluentSystemIcons-Regular</FontFamily>

            <!--
              WPF-UI puts a clear button inside every text box. It floats over
              the text at the right-hand edge — where the pointer travels when
              selecting to the end of a line — and one click empties the field.
              For a hostname, a key path or a note that is a small disaster and
              the button saves a keystroke, so it is off everywhere by default.
              Anything that genuinely wants one, like the search box, asks for
              it: new fields are then safe without anyone remembering to make
              them so.
            -->
            <Style TargetType="{x:Type ui:TextBox}" BasedOn="{StaticResource {x:Type ui:TextBox}}">
                <Setter Property="ClearButtonEnabled" Value="False" />
            </Style>

            <Style TargetType="{x:Type ui:PasswordBox}" BasedOn="{StaticResource {x:Type ui:PasswordBox}}">
                <Setter Property="ClearButtonEnabled" Value="False" />
            </Style>

            <!--
              WPF-UI's context menu slides in from 90 pixels above its final
              position, which reads as the menu falling into place rather than
              opening where it was asked for. The animation lives inside the
              control template, so the only way to drop it is to restate the
              template without the trigger that starts it. Everything else here
              matches the original.
            -->
            <Style TargetType="{x:Type ContextMenu}" BasedOn="{StaticResource UiContextMenu}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ContextMenu}">
                            <ui:EffectThicknessDecorator Thickness="30">
                                <Border Padding="0,3,0,3"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="20"
                                                          Direction="270"
                                                          Opacity="0.135"
                                                          ShadowDepth="10"
                                                          Color="#202020" />
                                    </Border.Effect>
                                    <StackPanel ClipToBounds="True"
                                                IsItemsHost="True"
                                                KeyboardNavigation.DirectionalNavigation="Cycle"
                                                Orientation="Vertical" />
                                </Border>
                            </ui:EffectThicknessDecorator>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
